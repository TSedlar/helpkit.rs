package rs.helpkit.api.rsui

import java.awt.Graphics2D

class RSTabContentPanel : RSWindow(0, 0) {

    override fun render(g: Graphics2D, rx: Int, ry: Int) {
//        g.color = Color.WHITE
//        g.fillRect(rx, ry, w, h)
    }
}